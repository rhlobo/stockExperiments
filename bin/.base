#!/bin/bash

export PROJECT_NAME='stockExperiments'

export CURRDIR="$(pwd)"
export PROJECT_LOCATION="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )"
export PYTHONPATH="${PROJECT_LOCATION}/${PROJECT_NAME}"
